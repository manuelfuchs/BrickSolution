using System;
using FakeItEasy;
using Microsoft.VisualStudio.TestTools.UnitTesting;

namespace BrickSolution.LogicTests
{
    [TestClass]
    public class RobotTests
    {
        [TestMethod]
        public void CheckBreakAfterSeconds()
        {
            //int counter = 0;
            //IRobot fakeRobot = A.Fake<IRobot>();

            //A.CallTo(() => fakeRobot.IRBreakCondition(A<object[]>.Ignored))
            //    .ReturnsLazily(() =>
            //    {
            //        counter++;
            //        return counter == 5000 ? true : false;
            //    });

            //fakeRobot.Drive(50, fakeRobot.IRBreakCondition, null);
        }
    }
}
